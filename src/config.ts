export const API_BASE_URL = 'http://localhost:3000/api';

export const API_ENDPOINTS = {
    SIGNUP: `${API_BASE_URL}/auth/signup`,
    TEST: `${API_BASE_URL}/test`,
    LOGIN: `${API_BASE_URL}/auth/login`,
    ADMIN: `${API_BASE_URL}/auth/admindashboard`,
};